package com.example.oopmap.Tiles;

import com.example.oopmap.Meta.TileMap;

public class DeepWaterTile extends WaterTile {
    public DeepWaterTile(TileMap map, Integer tx, Integer ty, double waterLevel) {
        super(map, tx, ty, waterLevel);
    }
}
