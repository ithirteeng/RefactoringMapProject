package com.example.oopmap.Tiles;

import com.example.oopmap.Meta.TileMap;

public class LowWaterTile extends WaterTile {
    public LowWaterTile(TileMap map, Integer tx, Integer ty, double waterLevel) {
        super(map, tx, ty, waterLevel);
    }
}
